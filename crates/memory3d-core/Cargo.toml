[package]
name = "memory3d-core"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
description = "Deterministic domain model for Memory3D"
license = "MIT"

[lib]
path = "src/lib.rs"

[dependencies]
rusqlite = { version = "0.37.0", features = ["bundled"] }
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.145"

[dev-dependencies]
tempfile = "3.20.0"

[lints]
workspace = true
